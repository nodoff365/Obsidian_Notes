### 해시함수의 보안 요구사항

1)역상저항성
	- 해시값이 확인된 상태에서 입력값을 구하는것은 계산적으로 불가능

2)제2역상저항성
	- 동일한 입력값(메시지)에 대해 서로 다른 해시값이 나와서는 안된다. 

3)충돌저항성
	- 같은 해시값을 생성하는 임의의 서로 다른 두 개의 입력값(M, M')을 찾는 것은 계산적으로 불가능해야 한다.

### 해시함수의 종류
