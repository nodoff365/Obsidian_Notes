## 

```bash
# 칼리 한글화
sudo su -
passwd It1
reboot

apt install -y fonts-nanum fcitx fcitx-hangul

vi .profile
LANG=ko_KR.UTF-8
LANGUAGE=ko_KR.UTF-8
```

```bash
# kali tool
maltego # 도메인 트리검색
```

```bash
# kali nmap
nmap -sT -v -O 10.0.0.31
nmap -sU -v -O 10.0.0.31
nmap -sT -v -O -p0-65535 10.0.0.31
nmap -sUV -v -O 10.0.0.31
nmap -sT -v -O -p0-65535 10.0.0.31 > metas3.txt
```

```bash

```