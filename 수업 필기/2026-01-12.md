___
/etc/group 
그룹 계정 wheel에 관리자 권한 계정 추가
___

# sudo 사용 시 비밀번호 x
___
sudo visudo
:se nu

107 #%wheel ALL=(ALL)       ALL
```맨 앞 # 추가```
110 %wheel  ALL=(ALL)       NOPASSWD: ALL
```맨 앞 # 삭제```
___

# vi 명령어
___
Ctrl+f : Page Down
x : 커서 위치 한 글자 삭제
:10 : 10번째 줄로 이동
___

https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/8/html/deploying_different_types_of_servers/setting-up-and-configuring-nginx_deploying-different-types-of-servers

ctrl+u 프롬프트 삭제

# 기본 패키지
```bash
sudo dnf -y update

sudo dnf -y upgrade

sudo dnf -y update && sudo dnf -y upgrade

sudo dnf -y install vim net-tools firewalld

sudo systemctl enable --now firewalld

sudo systemctl enable firewalld

sudo systemctl status firewalld

sudo systemctl restart firewalld

sudo systemctl stop firewalld

sudo systemctl start firewalld

```


```bash
firewall-cmd --list-ports
firewall-cmd --list-all
```


```bash
systemctl is-enabled nginx
systemctl status
```

TLS/SSL/SELinux 리포트 카페에 업로드
VMware nat 스위치 만들었을때 192.168.200.0
사용자 계정만들고 비밀번호 없이 ssh접속
192.168.9.  51.52.53

ls -lZ
ps -efZ

___
```bash
setenforce 1 or 0
getenforce
```

```bash
cd /etc/selinux
ls
vi config
```
---


```bash
groupadd -g 2000 developers
groupadd -g 3000 admin
cat /etc/group

useradd -d /home/developers/st610 -s /bin/bash -u 5000 -g developers st610
passwd [계정명]
cd /home
cat /etc/passwd
```

/bin
/usr/bin
/etc/skel  ```.bashrc 있는 경로```

```bash
~/Work/Shell_Script/[useradd.sh]
sh [useradd.sh] 
#[]는 파일명
```

```bash
groupadd -g 1500 dnsadmin
groupadd -g 1501 dnsops
groupadd -g 1502 dnsuser

useradd -d /home/dnsadmin/nsadmin -s /bin/bash -u 2000 -g 1500 nsadmin
useradd -d /home/dnsops/nsops01 -s /bin/bash -u 2001 -g 1501 nsops01
useradd -d /home/dnsuser/nsuser01 -s /bin/bash -u 2002 -g 1502 nsuser01
```


```bash
cd .ssh
ssh-keygen #ssh 키생성
ssh-copy-id [nsadmin@ns.kcci.edu] #[]로 키 복사
ssh [nsadmin@ns.kcci.edu] #비밀번호 없이 로그인
```