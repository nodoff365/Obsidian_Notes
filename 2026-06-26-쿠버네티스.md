
kubectl get namespaces
kubectl get pods -A
```bash
# rocky9-1
kubectl run app-nginx --image nginx --port=80

kubectl get pods

curl 192.168.104.1
<!DOCTYPE html>
<html>
<head>
<title>Welcome to nginx!</title>
<style>
html { color-scheme: light dark; }
body { width: 35em; margin: 0 auto;
font-family: Tahoma, Verdana, Arial, sans-serif; }
</style>
</head>
<body>
<h1>Welcome to nginx!</h1>
<p>If you see this page, nginx is successfully installed and working.
Further configuration is required for the web server, reverse proxy, 
API gateway, load balancer, content cache, or other features.</p>

<p>For online documentation and support please refer to
<a href="https://nginx.org/">nginx.org</a>.<br/>
To engage with the community please visit
<a href="https://community.nginx.org/">community.nginx.org</a>.<br/>
For enterprise grade support, professional services, additional 
security features and capabilities please refer to
<a href="https://f5.com/nginx">f5.com/nginx</a>.</p>

<p><em>Thank you for using nginx.</em></p>
</body>
</html>

kubectl describe pods app-nginx # 상세 정보 출력
kubectl logs app-nginx

kubectl expose pod app-nginx --type NodePort # pod nginx 외부노출
kubectl get services # 포트번호 확인
NAME         TYPE        CLUSTER-IP       EXTERNAL-IP   PORT(S)        AGE
app-nginx    NodePort    10.100.240.141   <none>        80:32555/TCP   35s

http://10.0.0.11:32555/ # 웹접속가능
http://10.0.0.12:32555/
http://10.0.0.13:32555/

kubectl delete service app-nginx # 삭제
kubectl get services # 삭제 확인
```

```bash
# 네임스페이스
kubectl get pods

kubectl get namespace # 네임스페이스 확인
kubectl get ns # kubectl api-resources 에서 별명 확인

delete namespace default # 기본 생성 네임스페이스 삭제 불가
kubectl get pods --namespace kube-system # kube-system 네임스페이스에 있는 파드들만 보여줌

# 팀별로 네임스페이스 사용 가능(리소스 격리 공간)
# 지정안하면 기본적으로 default에 생성
kubectl create namespace 1team
kubectl run app-nginx --image nginx --port 80 --namespace 1team #1team 네임스페이스에 nginx 생성
kubectl get pods --namespace 1team
kubectl delete pods app-nginx --namespace 1team
kubectl delete namespace 1team
```

```bash
# kubectl api-resources / apiVersion 확인

vi ns1.yml

---
apiVersion: v1
kind: Namespace
metadata:
  name: 1team
  labels:
    env: prod
---
apiVersion: v1
kind: Namespace
metadata:
  name: 2team
  labels:
    env: devel

kubectl apply -f ns1.yml # ns 1team, 2team 생성
```

```bash
# rocky9-docker
dnf -y install dnf-plugins-core
dnf config-manager --add-repo \
  https://download.docker.com/linux/centos/docker-ce.repo
dnf -y install docker-ce docker-ce-cli containerd.io    
systemctl enable --now docker

docker pull [이미지]

docker save -o all.tar alpine busybox httpd nginx rockylinux/rockylinux wordpress mysql:8.0

scp all.tar root@10.0.0.12:/root/
scp all.tar root@10.0.0.13:/root/
```

```bash
#rocky9-2
ctr -n k8s.io image import all.tar
```

```bash
#rocky9-3
ctr -n k8s.io image import all.tar
```

```bash
# rocker9-1
mkdir /pod
cd /pod
vi nginx.yml

apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    env: prod
    app: nginx
  namespace: 1team
spec:
  containers:
  - name: n1
    image: nginx
    imagePullPolicy: IfNotPresent
    ports:
    - containerPort: 80
      
vi apache.yml

apiVersion: v1
kind: Pod
matadata:
  name: apache
  labels:
    env: prod
    app: httpd
  namespace: 2team
spec:
  containers:
  - name: h1
    image: httpd
    imagePullPolicy: Never
    ports:
    - containerPort: 80
    
kubectl apply -f apache.yml
kubectl expose --name apa pod apache --type NodePort --namespace 2team

kubectl exec --namespace 2team -it apache -- /bin/bash
cat > htdocs/index.html << eof
> <html>
> <body>
> <h1>SGM-K8S-WEB</h1>
> </body>
> </html>
> eof




```

```bash
watch -n 2 kubectl get po --namespace 1team
kubectl get po,svc --namespace 2team -o wide
```


![[과제.png]]

```bash
vi ng1.yml

apiVersion: v1
kind: Namespace
metadata:
  name: 1team
---
apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: 1team
  labels:
    app: nginx
spec:
  containers:
  - name: n1
    image: nginx
    ports:
    - containerPort: 80

kubectl apply -f ng1.yml
kubectl expose pod nginx --name nginx --type NodePort --namespace 1team
kubectl exec --namespace 1team -it nginx -- /bin/bash -c 'cat > /usr/share/nginx/html/index.html << EOF
SGM-K8S-NGINX
EOF'
kubectl get svc,po --namespace 1team -o wide
```

```bash
vi ap1.yml

apiVersion: v1
kind: Namespace
metadata:
  name: 2team
---
apiVersion: v1
kind: Pod
metadata:
  name: apache
  namespace: 2team
  labels:
    app: apache
spec:
  containers:
  - name: a1
    image: httpd
    ports:
    - containerPort: 80
      
kubectl apply -f ap1.yml
kubectl expose pod apache --name apache --type NodePort --namespace 2team
kubectl exec --namespace 2team -it apache -- /bin/bash -c 'cat > /usr/local/apache2/htdocs/index.html << EOF
SGM-K8S-APACHE
EOF'
kubectl get svc,po --namespace 2team -o wide
```